A second attempt at a program to check the efficiency of repetive GET (so far) requests in context of multithreading, implemented with JMH.
Program can be configured in application.properties file under src/test/resources and run with command mvn jmh:benchmark. 

